# ==============================================================================
# asa-research-prototype — example environment file
# ------------------------------------------------------------------------------
# Copy this to `.env` and fill in your own values:
#     cp .env.example .env
#
# `.env` holds secrets and is gitignored — never commit it.
# This `.env.example` is committed as documentation, so keep placeholders only.
# ==============================================================================

# Nothing required yet — the scaffold reads no environment variables.
# Add entries as the prototype grows (LLM provider keys, service endpoints,
# device addresses), each with a link to where the value comes from.
