"""Core types and machinery shared by the agent's modules.

Holds what every other subpackage depends on and which depends on none of them —
configuration now, and the event, affect and expression types as they land.
"""
